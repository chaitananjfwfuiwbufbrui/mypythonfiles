def validate_pin(pin):
    #return true or false
    x =  pin.isdigit()
    if x == True :
        if len(pin) == 4:
            return True
        elif len(pin) == 6:
            return True
        else:
            return False
    else:
        return False
